build/bin
node_modules
frontend/dist

# Locally-built Wails binary (rebuild with `wails build`)
fabric

# Claude Code per-machine settings (not source)
.claude/

# Chisel design-system scanner cache (regenerates)
.chisel/

# macOS Finder metadata
.DS_Store
